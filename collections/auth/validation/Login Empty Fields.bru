meta {
  name: Login Empty Fields
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

headers {
  Accept: application/json
}

body:json {
  {}
}

assert {
  res.status: eq 200
}