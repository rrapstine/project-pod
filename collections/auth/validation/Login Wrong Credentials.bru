meta {
  name: Login Wrong Credentials
  type: http
  seq: 6
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

headers {
  Accept: application/json
}

body:json {
  {
    "email": "wrong@example.com",
    "password": "wrongpassword"
  }
}

assert {
  res.status: eq 200
}

docs {
  🌐 PUBLIC ROUTE - Testing invalid credentials
  
  Tests login endpoint with wrong credentials.
  This route doesn't require authentication but should reject invalid login attempts.
}
