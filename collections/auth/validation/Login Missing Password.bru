meta {
  name: Login Missing Password
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

headers {
  Accept: application/json
}

body:json {
  {
    "email": "test@example.com"
  }
}

assert {
  res.status: eq 200
}
