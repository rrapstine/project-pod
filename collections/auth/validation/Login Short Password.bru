meta {
  name: Login Short Password
  type: http
  seq: 5
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

headers {
  Accept: application/json
}

body:json {
  {
    "email": "test@example.com",
    "password": "123"
  }
}

assert {
  res.status: eq 200
}
